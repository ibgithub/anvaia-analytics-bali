server:
  port: 8082

spring:
  application:
    name: auth-service
  datasource:
    url: jdbc:postgresql://127.0.0.1:5432/cpi
    username: ml_user
    password: ml_user
  security:
    oauth2:
      client:
        registration:
          keycloak:
            client-id: ml-dashboard
            client-secret: wPPDQM0EIeQ8UNlnb1p3Qh73yIQMxVTl
            scope: openid, profile, email
            authorization-grant-type: authorization_code
            redirect-uri: "{baseUrl}/login/oauth2/code/keycloak"
        provider:
          keycloak:
            issuer-uri: https://aihub.bprks.local/auth/realms/master
jwt:
  secret: my-super-secret-key-minimum-32-bytes!!!
  expiration: 3600000 # 1 jam
springdoc:
  api-docs:
    path: /api-docs
  swagger-ui:
    path: /swagger
    operationsSorter: method
    tagsSorter: alpha
    display-request-duration: true

cors:
  allowed-origins: http://localhost:3000,http://localhost:5173,http://192.168.76.214:8080,https://192.168.76.214:8080